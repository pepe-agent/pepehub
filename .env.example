# GitHub OAuth App (Device Flow) — see README for how to create it.
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=

# Random long secret used to sign PepeHub session tokens.
# Generate one with: openssl rand -hex 32
SESSION_SECRET=
